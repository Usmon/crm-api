<?php

namespace {{ namespace }};

use Closure;
use Illuminate\Http\Request;

final class {{ class }}
{
    /**
     * @param Request $request
     * @param Closure $next
     *
     * @return mixed
     */
    public function handle(Request $request, Closure $next)
    {
        return $next($request);
    }
}
